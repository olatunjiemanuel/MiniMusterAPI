# MiniMusterAPI

A simple and lightweight web API built in C# (.NET 9) to manage muster/attendance data for small to medium-sized organisations.

## 🧰 Features

- RESTful API endpoints for CRUD operations on muster entries.  
- Built using .NET 9 (C#) and designed for easy extension.  
- Configurable via `appsettings.json` / `appsettings.Development.json`.  
- Clean project structure for maintainability (Project file: `MiniMusterAPI.csproj`, entry point: `Program.cs`).  
- Designed for integration into larger solutions (e.g., micro-services, corporate attendance systems).

## 🧩 Getting Started

### Prerequisites

- .NET 9 SDK installed  
- A development environment (Visual Studio, VS Code, Rider, or CLI)  
- (Optional) A SQL-backed database or other data store configured via `appsettings.json`

### Setup & Run

1. Clone the repository  
   ```bash
   git clone https://github.com/olatunjiemanuel/MiniMusterAPI.git
   cd MiniMusterAPI
dotnet build
dotnet run



